				<%= simple_form_for([@beer, @beer.reviews.build]) do |f| %>

					<div id = "rating-form" >
						<%= f.input :rating %>
					</div>
					<%= f.input :comment %>
					<%= f.button :submit, :class=>"btn-custom2"%>
				<% end %>