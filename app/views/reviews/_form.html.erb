				<%= simple_form_for([@beer, @beer.reviews.build]) do |f| %>

					<div id = "rating-form" >
						<label> Rating </label>
					</div>
					<%= f.input :comment %>
					<%= f.button :submit, :class=>"btn-custom2"%>
				<% end %>

<script>
	$(document).ready(function() {
	    $('#rating-form').raty('set'{
	    number: 5,
      path: '/assets/',
      scoreName: 'review[rating]'
    });
	 })
</script>